import { Controls, Canvas, Meta } from '@storybook/blocks';

import * as CardLoadingStories from './CardLoading.stories';

<Meta of={CardLoadingStories} />

# Loading

> Pronto para ser usado em qualquer lugar.

Um componente usado para indicar que um conteúdo está sendo carregado.

<Canvas of={CardLoadingStories.Default} />

<Controls />

Testeimport { Canvas, Controls, Meta } from '@storybook/blocks'import { Canvas, Controls, Meta } from '@storybook/blocks';

